#include<stdio.h>
#include<stdlib.h>
void pattern(int n)
{
    int i,j
    for(i=0;i<=n;i++)
}

void main()
{
    int n;
    printf("Enter number of rows : ");
    scanf("%d",&n);
    pattern(n);
    
}